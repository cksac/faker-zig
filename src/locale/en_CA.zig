pub const internet = struct {
    pub usingnamespace @import("../locales/en_CA/internet.zig");
};

pub const location = struct {
    pub usingnamespace @import("../locales/en_CA/location.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/en_CA/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/en_CA/person.zig");
};

pub const phone_number = struct {
    pub usingnamespace @import("../locales/en_CA/phone_number.zig");
};
