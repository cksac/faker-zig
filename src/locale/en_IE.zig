pub const cell_phone = struct {
    pub usingnamespace @import("../locales/en_IE/cell_phone.zig");
};

pub const internet = struct {
    pub usingnamespace @import("../locales/en_IE/internet.zig");
};

pub const location = struct {
    pub usingnamespace @import("../locales/en_IE/location.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/en_IE/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/en_IE/person.zig");
};

pub const phone_number = struct {
    pub usingnamespace @import("../locales/en_IE/phone_number.zig");
};
