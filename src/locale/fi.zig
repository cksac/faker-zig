pub const location = struct {
    pub usingnamespace @import("../locales/fi/location.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/fi/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/fi/person.zig");
};
