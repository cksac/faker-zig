pub const color = struct {
    pub usingnamespace @import("../locales/az/color.zig");
};

pub const commerce = struct {
    pub usingnamespace @import("../locales/az/commerce.zig");
};

pub const company = struct {
    pub usingnamespace @import("../locales/az/company.zig");
};

pub const date = struct {
    pub usingnamespace @import("../locales/az/date.zig");
};

pub const internet = struct {
    pub usingnamespace @import("../locales/az/internet.zig");
};

pub const location = struct {
    pub usingnamespace @import("../locales/az/location.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/az/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/az/person.zig");
};

pub const phone_number = struct {
    pub usingnamespace @import("../locales/az/phone_number.zig");
};
