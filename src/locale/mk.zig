pub const cell_phone = struct {
    pub usingnamespace @import("../locales/mk/cell_phone.zig");
};

pub const company = struct {
    pub usingnamespace @import("../locales/mk/company.zig");
};

pub const date = struct {
    pub usingnamespace @import("../locales/mk/date.zig");
};

pub const internet = struct {
    pub usingnamespace @import("../locales/mk/internet.zig");
};

pub const location = struct {
    pub usingnamespace @import("../locales/mk/location.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/mk/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/mk/person.zig");
};

pub const phone_number = struct {
    pub usingnamespace @import("../locales/mk/phone_number.zig");
};
