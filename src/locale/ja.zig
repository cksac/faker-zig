pub const cell_phone = struct {
    pub usingnamespace @import("../locales/ja/cell_phone.zig");
};

pub const company = struct {
    pub usingnamespace @import("../locales/ja/company.zig");
};

pub const location = struct {
    pub usingnamespace @import("../locales/ja/location.zig");
};

pub const lorem = struct {
    pub usingnamespace @import("../locales/ja/lorem.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/ja/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/ja/person.zig");
};

pub const phone_number = struct {
    pub usingnamespace @import("../locales/ja/phone_number.zig");
};
