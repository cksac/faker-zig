pub const cell_phone = struct {
    pub usingnamespace @import("../locales/de_AT/cell_phone.zig");
};

pub const company = struct {
    pub usingnamespace @import("../locales/de_AT/company.zig");
};

pub const internet = struct {
    pub usingnamespace @import("../locales/de_AT/internet.zig");
};

pub const location = struct {
    pub usingnamespace @import("../locales/de_AT/location.zig");
};

pub const metadata = struct {
    pub usingnamespace @import("../locales/de_AT/metadata.zig");
};

pub const person = struct {
    pub usingnamespace @import("../locales/de_AT/person.zig");
};

pub const phone_number = struct {
    pub usingnamespace @import("../locales/de_AT/phone_number.zig");
};

pub const word = struct {
    pub usingnamespace @import("../locales/de_AT/word.zig");
};
